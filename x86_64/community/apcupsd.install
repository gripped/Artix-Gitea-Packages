post_install() {
  sed -e 's,^EVENTSFILE,#EVENTSFILE,' \
      -e 's,^STATFILE,#STATFILE,' -i /etc/apcupsd/apcupsd.conf
}

pre_remove() {
    if [ -e "/run/service/apcupsd-srv" ]; then
        s6-rc -d change apcupsd
    fi
}

post_remove() {
    rm -rf /etc/s6/sv/apcupsd-log
}
