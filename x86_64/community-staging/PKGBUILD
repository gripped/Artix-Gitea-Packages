# Maintainer: Dudemanguy <dudemanguy@artixlinux.org>
# Contributor: Nathan <ndowens@artixlinux.org>
pkgname=boinc-s6
pkgver=20210511
pkgrel=1
pkgdesc="s6-rc service scripts for boinc"
arch=('any')
url="https://gitea.artixlinux.org/artix/s6-services"
groups=('s6-galaxy')
provides=('init-boinc')
conflicts=('init-boinc')
depends=('boinc' 's6-base')
makedepends=('git')
backup=('etc/s6/config/boinc.conf')
source=("git+https://gitea.artixlinux.org/artix/s6-services.git")
sha256sums=('SKIP')

package() {
    cd "${srcdir}"/s6-services
    sh install.sh "boinc" "${pkgdir}"
}
