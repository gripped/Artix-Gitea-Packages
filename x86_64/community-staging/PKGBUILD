# Maintainer: Nathan Owens <ndowens@artixlinux.org>
# Contributor: Dudemanguy <dudemanguy@artixlinux.org>
pkgname=gitea-s6
pkgver=20210511
pkgrel=1
pkgdesc="s6-rc service scripts for gitea"
arch=('any')
url="https://gitea.artixlinux.org/artix/s6-services"
groups=('s6-galaxy')
provides=('init-gitea')
conflicts=('init-gitea')
depends=('gitea' 's6-base')
makedepends=('git')
backup=('etc/s6/config/gitea.conf')
source=("git+https://gitea.artixlinux.org/artix/s6-services.git")
sha256sums=('SKIP')

package() {
    cd "${srcdir}"/s6-services
    sh install.sh "gitea" "${pkgdir}"
}
