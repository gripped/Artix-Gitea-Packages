pre_remove() {
    if [ -e "/run/service/transmission-daemon" ]; then
        s6-rc -d change transmission-daemon
    fi
}

post_remove() {
  rm -rf /etc/s6/sv/transmission-daemon-log
  rm -rf /etc/s6/sv/transmission-daemon-srv
}
