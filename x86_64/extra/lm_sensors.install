pre_remove() {
    if [ -e "/run/service/fancontrol" ]; then
        s6-rc -d change fancontrol
    fi
    if [ -e "/run/service/s6rc-oneshot-holder" ]; then
        s6-rc -d change lm_sensors
    fi
}
