pre_remove() {
    if [ -e "/run/service/nmbd-srv" ]; then
        s6-rc -d change nmbd
    fi
    if [ -e "/run/service/smbd-srv" ]; then
        s6-rc -d change smbd
    fi
}

post_remove() {
    rm -rf /etc/s6/sv/nmbd-log
    rm -rf /etc/s6/sv/smbd-log
}
