pre_remove() {
	if [ -e "/run/service/s6rc-oneshot-runner" ]; then
        s6-rc -d change auditctl
	fi

    if [ -e "/run/service/auditd-srv" ]; then
        s6-rc -d change auditd
    fi
}

post_remove() {
    rm -rf /etc/s6/sv/auditd-log
}
