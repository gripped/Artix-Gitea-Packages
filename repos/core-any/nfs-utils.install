pre_remove() {
    if [ -e "/run/service/nfs-server" ]; then
        s6-rc -d change nfs-server
    fi
    if [ -e "/run/service/statd" ]; then
        s6-rc -d change statd
	fi
}

post_remove() {
    rm -rf /etc/s6/sv/nfs-server-log
    rm -rf /etc/s6/sv/statd-log
}
