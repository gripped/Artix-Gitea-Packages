pre_remove() {
    if [ -e "/run/service/wpa_supplicant" ]; then
        s6-rc -d change wpa_supplicant
    fi
}

post_upgrade() {
	if [ -e "/etc/s6/sv/wpa_supplicant" ]; then
		rm -rf /etc/s6/sv/wpa_supplicant
	fi
}
