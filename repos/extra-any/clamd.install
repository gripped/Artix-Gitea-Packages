pre_remove() {
    if [ -e "/run/service/clamd" ]; then
        s6-rc -d change clamd
    fi
    if [ -e "/run/service/freshclam" ]; then
        s6-rc -d change freshclam
    fi
}

post_remove() {
    rm -rf /etc/s6/sv/clamd-log
    rm -rf /etc/s6/sv/freshclam-log
}
