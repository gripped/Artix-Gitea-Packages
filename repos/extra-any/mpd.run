#!/usr/bin/execlineb -P
foreground { install -d -m 0755 -o mpd -g mpd /run/mpd }
foreground { install -d -m 0755 /var/lib/mpd/playlists }
foreground { chown -R mpd:mpd /var/lib/mpd }
exec mpd --no-daemon
