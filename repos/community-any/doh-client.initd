#!/usr/bin/openrc-run

pidfile="/run/${RC_SVCNAME}.pid"
command="/usr/bin/doh-client"
command_args="-conf /etc/dns-over-https/doh-client.conf"
command_background_args="-pid-file ${pidfile}"
command_background=true

depend() {
	after net
	use dns logger
}
