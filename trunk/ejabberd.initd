#!/usr/bin/openrc-run

pidfile="/run/ejabberd.pid"
command="/usr/bin/ejabberdctl"
command_args="${EJABBERDCTL_OPT} start"

depend() {
	use dns
	need net
	provide jabber-server
}

stop() {
	start-stop-daemon --pidfile "$pidfile" --exec "$command" stop
		
}
