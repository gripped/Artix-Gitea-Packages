pre_remove() {
    if [ -e "/run/service/doh-server-srv" ]; then
        s6-rc -d change doh-server
    fi

    if [ -e "/run/service/doh-client-srv" ]; then
        s6-rc -d change doh-client
    fi
}
