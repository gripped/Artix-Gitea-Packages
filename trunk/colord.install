pre_remove() {
    if [ -e "/run/service/colord-srv" ]; then
        s6-rc -d change colord
    fi
    if [ -e "/run/service/colord-session-srv" ]; then
        s6-rc -d change colord-session
    fi
}
