pre_remove() {
    if [ -e "/run/service/postgresql-srv" ]; then
        s6-rc -d change postgresql
    fi
}

post_remove() {
    rm -rf /etc/s6/sv/postgresql-log
    rm -rf /etc/s6/sv/postgresql-srv
}
