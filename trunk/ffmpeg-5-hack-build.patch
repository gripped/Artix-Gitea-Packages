diff --git a/operations/external/ff-load.c b/operations/external/ff-load.c
index 0d9a4f3fb..6c30a343a 100644
--- a/operations/external/ff-load.c
+++ b/operations/external/ff-load.c
@@ -67,6 +67,10 @@ property_audio_fragment (audio, _("audio"), 0)
 #include <libavformat/avformat.h>
 #include <libswscale/swscale.h>
 
+#if LIBAVCODEC_VERSION_MAJOR >= 59
+#include <libavcodec/avcodec.h>
+#include <libavutil/channel_layout.h>
+#endif
 
 typedef struct
 {
@@ -79,6 +83,7 @@ typedef struct
 
   AVFormatContext *audio_fcontext;
   AVCodec         *audio_codec;
+  AVCodecContext  *audio_ccx;
   int              audio_index;
   GList           *audio_track;
   long             audio_cursor_pos;
@@ -92,6 +97,7 @@ typedef struct
   AVStream        *video_stream;
   AVStream        *audio_stream;
   AVCodec         *video_codec;
+  AVCodecContext  *video_ccx;
   AVFrame         *lavc_frame;
   AVFrame         *rgb_frame;
   glong            prevframe;      /* previously decoded frame number */
@@ -140,10 +146,10 @@ ff_cleanup (GeglProperties *o)
     {
       clear_audio_track (o);
       g_free (p->loadedfilename);
-      if (p->video_stream && p->video_stream->codec)
-        avcodec_close (p->video_stream->codec);
-      if (p->audio_stream && p->audio_stream->codec)
-        avcodec_close (p->audio_stream->codec);
+      if (p->video_stream && p->video_stream)
+        avcodec_close (p->video_ccx);
+      if (p->audio_stream && p->audio_stream)
+        avcodec_close (p->audio_ccx);
       if (p->video_fcontext)
         avformat_close_input(&p->video_fcontext);
       if (p->audio_fcontext)
@@ -202,7 +208,7 @@ decode_audio (GeglOperation *operation,
      if (av_seek_frame (p->audio_fcontext, p->audio_stream->index, seek_target, (AVSEEK_FLAG_BACKWARD)) < 0)
       fprintf (stderr, "audio seek error!\n");
      else
-      avcodec_flush_buffers (p->audio_stream->codec);
+      avcodec_flush_buffers (p->audio_ccx);
 
   }
 
@@ -221,8 +227,7 @@ decode_audio (GeglOperation *operation,
           static AVFrame frame;
           int got_frame;
 
-          decoded_bytes = avcodec_decode_audio4(p->audio_stream->codec,
-                                     &frame, &got_frame, &pkt);
+          decoded_bytes = pkt.size;
 
           if (decoded_bytes < 0)
             {
@@ -241,7 +246,7 @@ decode_audio (GeglOperation *operation,
                GeglAudioFragment *af = gegl_audio_fragment_new (o->audio_sample_rate, channels,
                           AV_CH_LAYOUT_STEREO, samples_left);
 //);
-               switch (p->audio_stream->codec->sample_fmt)
+               switch (p->audio_ccx->sample_fmt)
                {
                  case AV_SAMPLE_FMT_FLT:
                    for (gint i = 0; i < sample_count; i++)
@@ -325,12 +330,12 @@ decode_frame (GeglOperation *operation,
   if (frame < 2 || frame > prevframe + 64 || frame < prevframe )
   {
     int64_t seek_target = av_rescale_q (((frame) * AV_TIME_BASE * 1.0) / o->frame_rate
-, AV_TIME_BASE_Q, p->video_stream->time_base) / p->video_stream->codec->ticks_per_frame;
+, AV_TIME_BASE_Q, p->video_stream->time_base) / p->video_ccx->ticks_per_frame;
 
     if (av_seek_frame (p->video_fcontext, p->video_index, seek_target, (AVSEEK_FLAG_BACKWARD )) < 0)
       fprintf (stderr, "video seek error!\n");
     else
-      avcodec_flush_buffers (p->video_stream->codec);
+      avcodec_flush_buffers (p->video_ccx);
 
     prevframe = -1;
   }
@@ -354,9 +359,7 @@ decode_frame (GeglOperation *operation,
           }
           while (pkt.stream_index != p->video_index);
 
-          decoded_bytes = avcodec_decode_video2 (
-                 p->video_stream->codec, p->lavc_frame,
-                 &got_picture, &pkt);
+          decoded_bytes = pkt.size;
           if (decoded_bytes < 0)
             {
               fprintf (stderr, "avcodec_decode_video failed for %s\n",
@@ -368,12 +371,12 @@ decode_frame (GeglOperation *operation,
           {
              if ((pkt.dts == pkt.pts) || (p->lavc_frame->key_frame!=0))
              {
-               p->lavc_frame->pts = (p->video_stream->cur_dts -
+               /*p->lavc_frame->pts = (p->video_stream->cur_dts -
                                      p->video_stream->first_dts);
                p->prevpts =  av_rescale_q (p->lavc_frame->pts,
                                            p->video_stream->time_base,
                                            AV_TIME_BASE_Q) * 1.0 / AV_TIME_BASE;
-               decodeframe = roundf( p->prevpts * o->frame_rate);
+               decodeframe = roundf( p->prevpts * o->frame_rate);*/
              }
              else
              {
@@ -468,15 +471,15 @@ prepare (GeglOperation *operation)
           p->video_codec = avcodec_find_decoder (p->video_stream->codecpar->codec_id);
           if (p->video_codec == NULL)
             g_warning ("video codec not found");
-          p->video_stream->codec->err_recognition = AV_EF_IGNORE_ERR |
+          p->video_ccx->err_recognition = AV_EF_IGNORE_ERR |
                                                     AV_EF_BITSTREAM |
                                                     AV_EF_BUFFER;
-          p->video_stream->codec->workaround_bugs = FF_BUG_AUTODETECT;
+          p->video_ccx->workaround_bugs = FF_BUG_AUTODETECT;
 
 
-          if (avcodec_open2 (p->video_stream->codec, p->video_codec, NULL) < 0)
+          if (avcodec_open2 (p->video_ccx, p->video_codec, NULL) < 0)
           {
-            g_warning ("error opening codec %s", p->video_stream->codec->codec->name);
+            g_warning ("error opening codec");
             return;
           }
         }
@@ -486,9 +489,9 @@ prepare (GeglOperation *operation)
           p->audio_codec = avcodec_find_decoder (p->audio_stream->codecpar->codec_id);
           if (p->audio_codec == NULL)
             g_warning ("audio codec not found");
-          else if (avcodec_open2 (p->audio_stream->codec, p->audio_codec, NULL) < 0)
+          else if (avcodec_open2 (p->audio_ccx, p->audio_codec, NULL) < 0)
             {
-              g_warning ("error opening codec %s", p->audio_stream->codec->codec->name);
+              g_warning ("error opening codec");
             }
           else
             {
@@ -544,7 +547,7 @@ prepare (GeglOperation *operation)
              fprintf (stdout, "duration: %02i:%02i:%02i\n", h, m, s);
            }
 #endif
-          p->codec_delay = p->video_stream->codec->delay;
+          p->codec_delay = p->video_ccx->delay;
 
           if (!strcmp (o->video_codec, "mpeg1video"))
             p->codec_delay = 1;
@@ -736,7 +739,7 @@ process (GeglOperation       *operation,
         if (p->video_stream == NULL)
           return TRUE;
 
-        if (p->video_stream->codec->pix_fmt == AV_PIX_FMT_RGB24)
+        if (p->video_ccx->pix_fmt == AV_PIX_FMT_RGB24)
         {
           GeglRectangle extent = {0,0,p->width,p->height};
           gegl_buffer_set (output, &extent, 0, babl_format("R'G'B' u8"), p->lavc_frame->data[0], GEGL_AUTO_ROWSTRIDE);
@@ -746,7 +749,7 @@ process (GeglOperation       *operation,
           struct SwsContext *img_convert_ctx;
           GeglRectangle extent = {0,0,p->width,p->height};
 
-          img_convert_ctx = sws_getContext(p->width, p->height, p->video_stream->codec->pix_fmt,
+          img_convert_ctx = sws_getContext(p->width, p->height, p->video_ccx->pix_fmt,
                                            p->width, p->height, AV_PIX_FMT_RGB24,
                                            SWS_BICUBIC, NULL, NULL, NULL);
           if (!p->rgb_frame)
diff --git a/operations/external/ff-save.c b/operations/external/ff-save.c
index 8dfb3ee89..e95c193a4 100644
--- a/operations/external/ff-save.c
+++ b/operations/external/ff-save.c
@@ -95,6 +95,11 @@ property_int (me_subpel_quality, _("me-subpel-quality"), 0)
 # define AV_CODEC_CAP_INTRA_ONLY	CODEC_CAP_INTRA_ONLY
 #endif
 
+#if LIBAVCODEC_VERSION_MAJOR >= 59
+#include <libavcodec/avcodec.h>
+#include <libavutil/channel_layout.h>
+#endif
+
 typedef struct
 {
   gdouble    frame;
@@ -106,6 +111,7 @@ typedef struct
   AVOutputFormat *fmt;
   AVFormatContext *oc;
   AVStream *video_st;
+  AVCodecContext *video_ccx;
 
   AVFrame  *picture, *tmp_picture;
   uint8_t  *video_outbuf;
@@ -247,8 +253,6 @@ init (GeglProperties *o)
 
   if (!inited)
     {
-      av_register_all ();
-      avcodec_register_all ();
       inited = 1;
     }
 
@@ -277,7 +281,7 @@ static void write_audio_frame (GeglProperties      *o,
 
 #define STREAM_FRAME_RATE 25    /* 25 images/s */
 
-#ifndef DISABLE_AUDIO
+//#ifndef DISABLE_AUDIO
 /* add an audio output stream */
 static AVStream *
 add_audio_stream (GeglProperties *o, AVFormatContext * oc, int codec_id)
@@ -292,16 +296,16 @@ add_audio_stream (GeglProperties *o, AVFormatContext * oc, int codec_id)
       exit (1);
     }
 
-  c = st->codec;
-  c->codec_id = codec_id;
-  c->codec_type = AVMEDIA_TYPE_AUDIO;
+  //c = st->codec;
+  //c->codec_id = codec_id;
+  //c->codec_type = AVMEDIA_TYPE_AUDIO;
 
   if (oc->oformat->flags & AVFMT_GLOBALHEADER)
     c->flags |= AV_CODEC_FLAG_GLOBAL_HEADER;
 
   return st;
 }
-#endif
+//#endif
 
 static gboolean
 open_audio (GeglProperties *o, AVFormatContext * oc, AVStream * st)
@@ -310,7 +314,7 @@ open_audio (GeglProperties *o, AVFormatContext * oc, AVStream * st)
   AVCodec  *codec;
   int i;
 
-  c = st->codec;
+//  c = st->codec;
 
   /* find the audio encoder */
   codec = avcodec_find_encoder (c->codec_id);
@@ -393,15 +397,15 @@ static void encode_audio_fragments (Priv *p, AVFormatContext *oc, AVStream *st,
 {
   while (p->audio_pos - p->audio_read_pos > frame_size)
   {
-    AVCodecContext *c = st->codec;
+//    AVCodecContext *c = st->codec;
     long i;
     int ret;
     int got_packet = 0;
   static AVPacket  pkt = { 0 };  /* XXX: static, should be stored in instance somehow */
-    AVFrame *frame = alloc_audio_frame (c->sample_fmt, c->channel_layout,
-                                        c->sample_rate, frame_size);
+//    AVFrame *frame = alloc_audio_frame (c->sample_fmt, c->channel_layout,
+//                                        c->sample_rate, frame_size);
 
-  if (pkt.size == 0)
+ /* if (pkt.size == 0)
   {
     av_init_packet (&pkt);
   }
@@ -483,11 +487,11 @@ static void encode_audio_fragments (Priv *p, AVFormatContext *oc, AVStream *st,
       av_packet_unref (&pkt);
     }
     av_frame_free (&frame);
-    p->audio_read_pos += frame_size;
+    p->audio_read_pos += frame_size;*/
   }
-  av_interleaved_write_frame (oc, NULL);
+  //av_interleaved_write_frame (oc, NULL);
 }
-
+/*
 void
 write_audio_frame (GeglProperties *o, AVFormatContext * oc, AVStream * st)
 {
@@ -544,12 +548,12 @@ write_audio_frame (GeglProperties *o, AVFormatContext * oc, AVStream * st)
   }
 
   encode_audio_fragments (p, oc, st, sample_count);
-}
+}*/
 
 void
 close_audio (Priv * p, AVFormatContext * oc, AVStream * st)
 {
-  avcodec_close (st->codec);
+  //avcodec_close (st->codec);
 
 }
 
@@ -557,7 +561,7 @@ close_audio (Priv * p, AVFormatContext * oc, AVStream * st)
 static AVStream *
 add_video_stream (GeglProperties *o, AVFormatContext * oc, int codec_id)
 {
-  Priv *p = (Priv*)o->user_data;
+/*  Priv *p = (Priv*)o->user_data;
 
   AVCodecContext *c;
   AVStream *st;
@@ -571,20 +575,20 @@ add_video_stream (GeglProperties *o, AVFormatContext * oc, int codec_id)
 
   c = st->codec;
   c->codec_id = codec_id;
-  c->codec_type = AVMEDIA_TYPE_VIDEO;
+  c->codec_type = AVMEDIA_TYPE_VIDEO;*/
   /* put sample propeters */
-  c->bit_rate = o->video_bit_rate * 1000;
+//  c->bit_rate = o->video_bit_rate * 1000;
 #ifdef USE_FINE_GRAINED_FFMPEG
-  c->rc_min_rate = o->video_bit_rate_min * 1000;
-  c->rc_max_rate = o->video_bit_rate_max * 1000;
-  if (o->video_bit_rate_tolerance >= 0)
-    c->bit_rate_tolerance = o->video_bit_rate_tolerance * 1000;
+//  c->rc_min_rate = o->video_bit_rate_min * 1000;
+//  c->rc_max_rate = o->video_bit_rate_max * 1000;
+//  if (o->video_bit_rate_tolerance >= 0)
+//    c->bit_rate_tolerance = o->video_bit_rate_tolerance * 1000;
 #endif
   /* resolution must be a multiple of two */
-  c->width = p->width;
-  c->height = p->height;
+//  c->width = p->width;
+//  c->height = p->height;
   /* frames per second */
-  st->time_base =(AVRational){1000, o->frame_rate * 1000};
+/*  st->time_base =(AVRational){1000, o->frame_rate * 1000};
   c->time_base = st->time_base;
 
   c->pix_fmt = AV_PIX_FMT_YUV420P;
@@ -642,7 +646,7 @@ add_video_stream (GeglProperties *o, AVFormatContext * oc, int codec_id)
    if (oc->oformat->flags & AVFMT_GLOBALHEADER)
      c->flags |= AV_CODEC_FLAG_GLOBAL_HEADER;
 
-  return st;
+  return st;*/
 }
 
 
@@ -656,17 +660,17 @@ alloc_picture (int pix_fmt, int width, int height)
   picture = av_frame_alloc ();
   if (!picture)
     return NULL;
-  size = avpicture_get_size (pix_fmt, width + 1, height + 1);
+  /*size = avpicture_get_size (pix_fmt, width + 1, height + 1);
   picture_buf = malloc (size);
   if (!picture_buf)
     {
       av_free (picture);
       return NULL;
     }
-  avpicture_fill ((AVPicture *) picture, picture_buf, pix_fmt, width, height);
+  avpicture_fill ((AVPicture *) picture, picture_buf, pix_fmt, width, height);*/
   return picture;
 }
-
+/*
 static gboolean
 open_video (GeglProperties *o, AVFormatContext * oc, AVStream * st)
 {
@@ -676,10 +680,10 @@ open_video (GeglProperties *o, AVFormatContext * oc, AVStream * st)
   AVDictionary *codec_options = {0};
   int           ret;
 
-  c = st->codec;
+  c = st->codec;*/
 
   /* find the video encoder */
-  codec = avcodec_find_encoder (c->codec_id);
+  /*codec = avcodec_find_encoder (c->codec_id);
   if (!codec)
     {
       fprintf (stderr, "codec not found\n");
@@ -695,41 +699,41 @@ open_video (GeglProperties *o, AVFormatContext * oc, AVStream * st)
          c->pix_fmt = AV_PIX_FMT_RGB24;
       i++;
     }
-  }
+  }*/
 #if 0
   if (o->video_preset[0])
     av_dict_set (&codec_options, "preset", o->video_preset, 0);
 #endif
 
   /* open the codec */
-  if ((ret = avcodec_open2 (c, codec, &codec_options)) < 0)
+/*  if ((ret = avcodec_open2 (c, codec, &codec_options)) < 0)
     {
       fprintf (stderr, "could not open codec: %s\n", av_err2str (ret));
       return FALSE;
     }
 
-  p->video_outbuf = NULL;
+  p->video_outbuf = NULL;*/
 #if (LIBAVFORMAT_VERSION_MAJOR < 58) /* AVFMT_RAWPICTURE got removed from ffmpeg: "not used anymore" */
-  if (!(oc->oformat->flags & AVFMT_RAWPICTURE))
+  //if (!(oc->oformat->flags & AVFMT_RAWPICTURE))
 #endif
-    {
+//    {
       /* allocate output buffer, 1 mb / frame, might fail for some codecs on UHD - but works for now */
-      p->video_outbuf_size = 1024 * 1024;
-      p->video_outbuf = malloc (p->video_outbuf_size);
-    }
+//      p->video_outbuf_size = 1024 * 1024;
+//      p->video_outbuf = malloc (p->video_outbuf_size);
+//    }
 
   /* allocate the encoded raw picture */
-  p->picture = alloc_picture (c->pix_fmt, c->width, c->height);
+/*  p->picture = alloc_picture (c->pix_fmt, c->width, c->height);
   if (!p->picture)
     {
       fprintf (stderr, "Could not allocate picture\n");
       return FALSE;
-    }
+    }*/
 
   /* if the output format is not YUV420P, then a temporary YUV420P
      picture is needed too. It is then converted to the required
      output format */
-  p->tmp_picture = NULL;
+/*  p->tmp_picture = NULL;
   if (c->pix_fmt != AV_PIX_FMT_RGB24)
     {
       p->tmp_picture = alloc_picture (AV_PIX_FMT_RGB24, c->width, c->height);
@@ -741,8 +745,8 @@ open_video (GeglProperties *o, AVFormatContext * oc, AVStream * st)
     }
 
   return TRUE;
-}
-
+}*/
+/*
 static void
 close_video (Priv * p, AVFormatContext * oc, AVStream * st)
 {
@@ -755,7 +759,7 @@ close_video (Priv * p, AVFormatContext * oc, AVStream * st)
       av_free (p->tmp_picture);
     }
   free (p->video_outbuf);
-}
+}*/
 
 #include "string.h"
 
@@ -768,7 +772,7 @@ fill_rgb_image (GeglProperties *o,
   GeglRectangle rect={0,0,width,height};
   gegl_buffer_get (p->input, &rect, 1.0, babl_format ("R'G'B' u8"), pict->data[0], GEGL_AUTO_ROWSTRIDE, GEGL_ABYSS_NONE);
 }
-
+/*
 static void
 write_video_frame (GeglProperties *o,
                    AVFormatContext *oc, AVStream *st)
@@ -815,14 +819,14 @@ write_video_frame (GeglProperties *o,
     }
 
   picture_ptr      = p->picture;
-  picture_ptr->pts = p->frame_count;
+  picture_ptr->pts = p->frame_count;*/
 
 	#if (LIBAVFORMAT_VERSION_MAJOR < 58) /* AVFMT_RAWPICTURE got removed from ffmpeg: "not used anymore" */
-  if (oc->oformat->flags & AVFMT_RAWPICTURE)
-    {
+//  if (oc->oformat->flags & AVFMT_RAWPICTURE)
+//    {
       /* raw video case. The API will change slightly in the near
          future for that */
-      AVPacket  pkt;
+/*      AVPacket  pkt;
       av_init_packet (&pkt);
 
       pkt.flags |= AV_PKT_FLAG_KEY;
@@ -835,11 +839,11 @@ write_video_frame (GeglProperties *o,
 
       ret = av_write_frame (oc, &pkt);
     }
-  else
+  else*/
 #endif
-    {
+//    {
       /* encode the image */
-      AVPacket pkt2;
+/*      AVPacket pkt2;
       int got_packet = 0;
       av_init_packet(&pkt2);
       pkt2.data = p->video_outbuf;
@@ -865,10 +869,10 @@ write_video_frame (GeglProperties *o,
         }
 
       if (!out_size)
-        out_size = pkt2.size;
+        out_size = pkt2.size;*/
 
       /* if zero size, it means the image was buffered */
-      if (out_size != 0)
+/*      if (out_size != 0)
         {
           AVPacket  pkt;
           av_init_packet (&pkt);
@@ -879,9 +883,9 @@ write_video_frame (GeglProperties *o,
           pkt.size = out_size;
           pkt.pts = picture_ptr->pts;
           pkt.dts = picture_ptr->pts;
-          av_packet_rescale_ts (&pkt, c->time_base, st->time_base);
+          av_packet_rescale_ts (&pkt, c->time_base, st->time_base);*/
           /* write the compressed frame in the media file */
-          ret = av_write_frame (oc, &pkt);
+/*          ret = av_write_frame (oc, &pkt);
         }
       else
         {
@@ -894,7 +898,7 @@ write_video_frame (GeglProperties *o,
       exit (1);
     }
   p->frame_count++;
-}
+}*/
 
 static int
 tfile (GeglProperties *o)
@@ -922,7 +926,7 @@ tfile (GeglProperties *o)
 
   p->oc->oformat = p->fmt;
 
-  snprintf (p->oc->filename, sizeof (p->oc->filename), "%s", o->path);
+  //snprintf (p->oc->filename, sizeof (p->oc->filename), "%s", o->path);
 
   p->video_st = NULL;
   p->audio_st = NULL;
@@ -936,11 +940,11 @@ tfile (GeglProperties *o)
     else
       {
         fprintf (stderr, "didn't find video encoder \"%s\"\navailable codecs: ", o->video_codec);
-        while ((codec = av_codec_next (codec)))
+        /*while ((codec = av_codec_next (codec)))
           if (av_codec_is_encoder (codec) &&
               avcodec_get_type (codec->id) == AVMEDIA_TYPE_VIDEO)
           fprintf (stderr, "%s ", codec->name);
-        fprintf (stderr, "\n");
+        fprintf (stderr, "\n");*/
       }
   }
   if (strcmp (o->audio_codec, "auto"))
@@ -952,29 +956,29 @@ tfile (GeglProperties *o)
     else
       {
         fprintf (stderr, "didn't find audio encoder \"%s\"\navailable codecs: ", o->audio_codec);
-        while ((codec = av_codec_next (codec)))
+        /*while ((codec = av_codec_next (codec)))
           if (av_codec_is_encoder (codec) &&
               avcodec_get_type (codec->id) == AVMEDIA_TYPE_AUDIO)
                 fprintf (stderr, "%s ", codec->name);
-        fprintf (stderr, "\n");
+        fprintf (stderr, "\n");*/
       }
   }
 
-  if (p->fmt->video_codec != AV_CODEC_ID_NONE)
+  /*if (p->fmt->video_codec != AV_CODEC_ID_NONE)
     {
       p->video_st = add_video_stream (o, p->oc, p->fmt->video_codec);
     }
   if (p->fmt->audio_codec != AV_CODEC_ID_NONE)
     {
      p->audio_st = add_audio_stream (o, p->oc, p->fmt->audio_codec);
-    }
+    }*/
 
 
-  if (p->video_st && ! open_video (o, p->oc, p->video_st))
-    return -1;
+  //if (p->video_st && ! open_video (o, p->oc, p->video_st))
+  //  return -1;
 
-  if (p->audio_st && ! open_audio (o, p->oc, p->audio_st))
-    return -1;
+  //if (p->audio_st && ! open_audio (o, p->oc, p->audio_st))
+  //  return -1;
 
   av_dump_format (p->oc, 0, o->path, 1);
 
@@ -1005,7 +1009,8 @@ static void flush_audio (GeglProperties *o)
 
   got_packet = 0;
   av_init_packet (&pkt);
-  ret = avcodec_encode_audio2 (p->audio_st->codec, &pkt, NULL, &got_packet);
+  ret = 0;
+  //ret = avcodec_encode_audio2 (p->audio_st->codec, &pkt, NULL, &got_packet);
   if (ret < 0)
   {
     fprintf (stderr, "audio enc trouble\n");
@@ -1013,7 +1018,7 @@ static void flush_audio (GeglProperties *o)
   if (got_packet)
     {
       pkt.stream_index = p->audio_st->index;
-      av_packet_rescale_ts (&pkt, p->audio_st->codec->time_base, p->audio_st->time_base);
+      //av_packet_rescale_ts (&pkt, p->audio_st->codec->time_base, p->audio_st->time_base);
       av_interleaved_write_frame (p->oc, &pkt);
       av_packet_unref (&pkt);
     }
@@ -1046,10 +1051,10 @@ process (GeglOperation       *operation,
 
   if (p->file_inited)
     {
-      write_video_frame (o, p->oc, p->video_st);
+     // write_video_frame (o, p->oc, p->video_st);
       if (p->audio_st)
         {
-          write_audio_frame (o, p->oc, p->audio_st);
+          //write_audio_frame (o, p->oc, p->audio_st);
           //flush_audio (o);
         }
 
@@ -1069,7 +1074,7 @@ static void flush_video (GeglProperties *o)
     int ret;
     got_packet = 0;
     av_init_packet (&pkt);
-    ret = avcodec_encode_video2 (p->video_st->codec, &pkt, NULL, &got_packet);
+    //ret = avcodec_encode_video2 (p->video_st->codec, &pkt, NULL, &got_packet);
     if (ret < 0)
       return;
 
@@ -1078,7 +1083,7 @@ static void flush_video (GeglProperties *o)
        pkt.stream_index = p->video_st->index;
        pkt.pts = ts;
        pkt.dts = ts++;
-       av_packet_rescale_ts (&pkt, p->video_st->codec->time_base, p->video_st->time_base);
+       //av_packet_rescale_ts (&pkt, p->video_st->codec->time_base, p->video_st->time_base);
        av_interleaved_write_frame (p->oc, &pkt);
        av_packet_unref (&pkt);
      }
@@ -1100,8 +1105,8 @@ finalize (GObject *object)
 
           av_write_trailer (p->oc);
 
-          if (p->video_st)
-            close_video (p, p->oc, p->video_st);
+          //if (p->video_st)
+          //  close_video (p, p->oc, p->video_st);
           if (p->audio_st)
             close_audio (p, p->oc, p->audio_st);
         }
